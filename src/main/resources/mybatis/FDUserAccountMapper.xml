<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xnjr.moom.dao.IFDUserAccountDAO">
	<resultMap id="fdUserAccount_DB_Result" type="fdUserAccount">
		<result column="user_id" property="userId" />
		<result column="real_name" property="realName" />		
		<result column="account_number" property="accountNumber" />
		<result column="status" property="status" />
		<result column="amount" property="amount" />
		
		<result column="frozen_amount" property="frozenAmount" />
		<result column="currency" property="currency" />		
		<result column="create_datetime" property="createDatetime" />	
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">	
			<if test="userId != null and userId != '' ">
				AND t.user_id = #{userId}
			</if>	
			<if test="realName != null and realName != '' ">
				AND t.real_name = #{realName}
			</if>	
			<if test="accountNumber != null and accountNumber != '' ">
				AND t.account_number = #{accountNumber}
			</if>
			<if test="status != null and status != '' ">
				AND t.status = #{status}
			</if>
			<if test="createDatetimeStart != null and createDatetimeStart != '' ">
				<![CDATA[AND t.create_datetime >= #{createDatetimeStart}]]>
			</if>
			<if test="createDatetimeEnd != null and createDatetimeEnd != '' ">
				<![CDATA[AND t.create_datetime <= #{createDatetimeEnd}]]>
			</if>
		</trim>
	</sql>
	
	<select id="select_fdUserAccount" parameterType="fdUserAccount" resultMap="fdUserAccount_DB_Result">
		SELECT 
		t.user_id
		,t.real_name
		,t.account_number
		,t.status
		,t.amount
		
		,t.frozen_amount
		,t.currency
		,t.create_datetime
		FROM
		tfd_user_account t 
		<include refid="where_condition"/>
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_fdUserAccount_count" parameterType="fdUserAccount" resultType="java.lang.Long">
		SELECT count(1) FROM tfd_user_account t 
		<include refid="where_condition"/>
	</select>

	<insert id="insert_fdUserAccount" parameterType="fdUserAccount">
		INSERT INTO tfd_user_account(
		user_id
		,real_name
		,account_number
		,status
		,amount
		
		,frozen_amount
		,currency
		,md5
		,create_datetime
		)
		VALUES(
		#{userId}
		,#{realName}
		,#{accountNumber}
		,#{status}
		,#{amount}
		
		,#{frozenAmount}
		,#{currency}
		,#{md5}
		,#{createDatetime}
		)
	</insert>
	<update id="update_fdUserAccountAmount" parameterType="fdUserAccount">
		UPDATE tfd_user_account
		SET
		 amount= #{amount}
		,md5 = #{md5}
		,update_datetime = #{updateDatetime}
		WHERE
		account_number = #{accountNumber}			
	</update>
	<update id="update_fdUserAccountFrozenAmount" parameterType="fdUserAccount">
		UPDATE tfd_user_account
		SET
		 amount= #{amount}
		,frozen_amount = #{frozenAmount}
		,md5 = #{md5}
		,update_datetime = #{updateDatetime}
		WHERE
		account_number = #{accountNumber}			
	</update>
	<update id="update_fdUserAccountStatus" parameterType="fdUserAccount">
		UPDATE tfd_user_account
		SET
		 status= #{status}
		,update_datetime = #{updateDatetime}
		WHERE
		account_number = #{accountNumber}		
	</update>
	
	<update id="update_fdUserAccountRealName" parameterType="fdUserAccount">
		UPDATE tfd_user_account
		SET
		 real_name= #{realName}
		 ,update_datetime = #{updateDatetime}
		WHERE
		user_id = #{userId}		
	</update>
</mapper>
