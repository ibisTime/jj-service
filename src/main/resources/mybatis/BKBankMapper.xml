<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xnjr.moom.dao.IBKBankDAO">
	<resultMap id="bkBank_DB_Result" type="bkBank">
		<result column="code" property="code" />
		<result column="name" property="name" />
		<result column="type" property="type" />
		<result column="url1" property="url1" />
		<result column="url2" property="url2" />
		<result column="status" property="status" />
		<result column="remark" property="remark" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != '' ">
				AND t.code = #{code}
			</if>
			<if test="name != null and name != '' ">
				AND t.name = #{name}
			</if>
			<if test="type != null and type != '' ">
				AND t.type = #{type}
			</if>
			<if test="status != null and status != '' ">
				AND t.status = #{status}
			</if>
		</trim>
	</sql>
	<select id="select_bkBank" parameterType="bkBank" resultMap="bkBank_DB_Result">
		SELECT
		code
		,name
		,type
		,url1
		,url2		
		,status
		,remark
		FROM
		tbk_bank t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				#{order}
			</if>
		</trim>
	</select>

	<select id="select_bkBank_count" parameterType="bkBank"
		resultType="java.lang.Long">
		SELECT count(1) FROM tbk_bank t
		<include refid="where_condition" />
	</select>

	<insert id="insert_bkBank" parameterType="bkBank">
		INSERT INTO
		tbk_bank(
		code
		,name
		,type
		,url1
		,url2		
		,status
		,remark
		)
		VALUES(
		#{code}
		,#{name}
		,#{type}
		,#{url1}
		,#{url2}
		,#{status}
		,#{remark}
		)
	</insert>
	
	<update id="update_bkBank" parameterType="bkBank">
		UPDATE tbk_bank
		SET
		name= #{name}
		,type= #{type}
		,url1= #{url1}
		,url2= #{url2}
		,status = #{status}
		,remark = #{remark}
		WHERE code = #{code}
	</update>

	<delete id="delete_bkBank" parameterType="bkBank">
		DELETE FROM
		tbk_bank
		WHERE
		code = #{code}
	</delete>
</mapper>
