<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xnjr.mall.dao.IRepairOrderDAO">
	<resultMap id="repairOrder_DB_Result" type="repairOrder">
		<result column="code" property="code" />
		<result column="goods_code" property="goodsCode" />
		<result column="model_name" property="modelName" />
		<result column="product_name" property="productName" />
		<result column="user_id" property="userId" />
		<result column="apply_user" property="applyUser" />
		<result column="contact" property="contact" />
		<result column="apply_reason" property="applyReason" />
		<result column="apply_datetime" property="applyDatetime" />
		<result column="status" property="status" />
		<result column="updater" property="updater" />
		<result column="update_datetime" property="updateDatetime" />
		<result column="remark" property="remark" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != '' ">
				AND t.code = #{code}
			</if>
			<if test="goodsCode != null and goodsCode != '' ">
				AND t.goods_code like "%"#{goodsCode}"%"
			</if>
			<if test="userId != null and userId != '' ">
				AND t.user_id = #{userId}
			</if>
			<if test="applyUser != null and applyUser != '' ">
				AND t.apply_user = #{applyUser}
			</if>
			<choose>
				 <when test="status != null and status != '' ">
				  AND t.status = #{status}
				 </when>
				 <otherwise>
				  AND 1 = 1
				 </otherwise>
			</choose>
			<if test="updater != null and updater != '' ">
				AND t.updater = #{updater}
			</if>
		</trim>
	</sql>
	<select id="select_repairOrder" parameterType="repairOrder" resultMap="repairOrder_DB_Result">
		SELECT
		code
		,goods_code
		,model_name
		,product_name
		,user_id
		,apply_user
		,contact
		,apply_reason
		,apply_datetime
		,status
		,updater
		,update_datetime
		,remark
		FROM
		tmall_repair_order t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				${order}
			</if>
		</trim>
	</select>

	<select id="select_repairOrder_count" parameterType="repairOrder"
		resultType="java.lang.Long">
		SELECT count(1) FROM tmall_repair_order t
		<include refid="where_condition" />
	</select>

	<insert id="insert_repairOrder" parameterType="repairOrder">
		INSERT INTO
		tmall_repair_order(
		code
		,goods_code
		,model_name
		,product_name
		,user_id
		,apply_user
		,contact
		,apply_reason
		,apply_datetime
		,status
		)
		VALUES(
		#{code}
		,#{goodsCode}
		,#{modelName}
		,#{productName}
		,#{userId}
		,#{applyUser}
		,#{contact}
		,#{applyReason}
		,#{applyDatetime}
		,#{status}
		)
	</insert>
	
	<update id="update_status" parameterType="repairOrder">
		UPDATE tmall_repair_order
		SET
		status = #{status}
		,updater = #{updater}
		,update_datetime = #{updateDatetime}
		,remark = #{remark}
		WHERE code = #{code}
	</update>

</mapper>
