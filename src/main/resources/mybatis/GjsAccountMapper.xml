<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xnjr.moom.dao.IGjsAccountDAO">
	<resultMap id="gjsAccount_DB_Result" type="gjsAccount">
		<result column="account_number" property="accountNumber" />
		<result column="status" property="status" />
		<result column="amount" property="amount" />
		<result column="frozen_amount" property="frozenAmount" />
		<result column="currency" property="currency" />		

		<result column="create_datetime" property="createDatetime" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">			
			<if test="accountNumber != null and accountNumber != '' ">
				AND t.account_number = #{accountNumber}
			</if>
			<if test="status != null and status != '' ">
				AND t.status = #{status}
			</if>	
			<if test="createDatetimeStart != null and createDatetimeStart != '' ">
				<![CDATA[AND t.create_datetime >= #{createDatetimeStart}]]>
			</if>
			<if test="createDatetimeEnd != null and createDatetimeEnd != '' ">
				<![CDATA[AND t.create_datetime <= #{createDatetimeEnd}]]>
			</if>					
		</trim>
	</sql>
	<select id="select_gjsAccount" parameterType="gjsAccount" resultMap="gjsAccount_DB_Result">
		SELECT 
		account_number
		,status
		,amount
		,frozen_amount
		,currency
		
		,create_datetime
		FROM
		tmoom_gjs_account t
		<include refid="where_condition"/>
		ORDER BY t.create_datetime DESC	
	</select>

	<select id="select_gjsAccount_count" parameterType="gjsAccount" resultType="java.lang.Long">
		SELECT count(1) FROM tmoom_gjs_account t
		<include refid="where_condition"/>
	</select>

	<insert id="insert_gjsAccount" parameterType="gjsAccount">
		INSERT INTO tmoom_gjs_account(
		account_number
		,amount
		,frozen_amount
		,currency
		,status
		
		,md5
		,create_datetime
		)
		VALUES(
		#{accountNumber}
		,#{amount}
		,#{frozenAmount}
		,#{currency}
		,#{status}
		
		,#{md5}
		,#{createDatetime}
		)
	</insert>
	
	<update id="update_gjsAccountAmount" parameterType="gjsAccount">
		UPDATE tmoom_gjs_account
		SET
		 amount= #{amount}
		,md5 = #{md5}
		,update_datetime = #{updateDatetime}
		WHERE
		account_number = #{accountNumber}			
	</update>
	
	<update id="update_gjsAccountFrozenAmount" parameterType="gjsAccount">
		UPDATE tmoom_gjs_account
		SET
		frozen_amount = #{frozenAmount}
		,update_datetime = #{updateDatetime}
		WHERE
		account_number = #{accountNumber}			
	</update>
	<update id="update_gjsAccountStatus" parameterType="gjsAccount">
		UPDATE tmoom_gjs_account
		SET
		 status= #{status}
		,update_datetime = #{updateDatetime}
		WHERE
		account_number = #{accountNumber}		
	</update>
</mapper>
